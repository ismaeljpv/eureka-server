server:
  port: 8761

spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8000

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

info:
  app:
    name: eureka-server